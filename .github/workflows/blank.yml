name: My First Workflow

on:
  push:
    branches:
      - main
      - feature-codeql

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Print Hello
        run: echo "Hello Srinivas! GitHub Actions is working."
